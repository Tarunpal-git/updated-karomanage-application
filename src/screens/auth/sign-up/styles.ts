import { StyleSheet } from "react-native";
import { COLORS } from "../../../colors";

export const styles = StyleSheet.create({
  title: {
    fontSize: 20,
    color: COLORS.black,
    marginVertical: 20,
  },
  description: {
    fontSize: 16,
    color: COLORS.black,
    textAlign: "center",
    lineHeight: 24,
  },
});
